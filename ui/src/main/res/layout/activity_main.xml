<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg"
    android:fitsSystemWindows="true">

    <!-- State-tinted background: graphite always underneath, teal fades in
         on top of it when the tunnel comes up. MainActivity animates the
         alpha of bgStateOn. -->
    <View
        android:id="@+id/bgStateOff"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_state_off"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <View
        android:id="@+id/bgStateOn"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_state_on"
        android:alpha="0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Fragment container -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomNav"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Floating icons-only navigation pill -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginStart="22dp"
        android:layout_marginEnd="22dp"
        android:layout_marginBottom="22dp"
        android:background="@drawable/bg_nav_glass"
        android:elevation="12dp"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        app:labelVisibilityMode="unlabeled"
        app:itemIconSize="22dp"
        app:elevation="0dp"
        app:itemRippleColor="@color/glass_lo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Unread-notifications dot, hovering over the profile tab (rightmost of
         four). Custom rather than a Material badge, which mis-anchors and
         renders gray under the AppCompat theme. -->
    <View
        android:id="@+id/navDot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:background="@drawable/bg_badge_dot_on"
        android:backgroundTint="@color/magenta"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/bottomNav"
        app:layout_constraintEnd_toEndOf="@id/bottomNav"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="52dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
